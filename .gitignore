secrets.ini

# --- Python bytecode / cache (do not version; regenerated by interpreter) ---
__pycache__/
*.py[cod]
*$py.class
*.pyo

# --- AI / IDE (do not version; local or generated) ---
.cursor/

# --- Content from updating (see docs/UPDATING.md) ---
# Extract output (if scripts/extract.py is run from repo root)
/res_extract/
# Generated ships data: scripts/ships/generate.py writes to generated/ (see generated/.gitignore);
# then moved to bot assets per UPDATING.md — do not version these
/bot/assets/public/ships.json
/bot/assets/public/ships_silhouettes/*
!/bot/assets/public/ships_silhouettes/.gitignore
/tools/
